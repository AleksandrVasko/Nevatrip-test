// 1.удалить первый и последний элемент строки

/* function removeChar(str){
    str = str.toLowerCase().split('');
    let str1 = str.splice(0, 1);
    str1 = str.reverse();
    str1 = str.splice(0, 1);
    str1 = toString();
    console.log(str)
   }
   removeChar('alex')
 */

/* function removeChar(str) {
    console.log(str.slice(1, -1))
}
removeChar('alex') */

// 2. Напишите функцию, которая вычисляет среднее значение чисел в заданном списке. Пустые массивы должны возвращать 0.

function findAverage(array) {
    
    return 0;
}